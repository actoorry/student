<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="vip.appap.suxin.module.sales.dal.mysql.SalesPartnerStatisticsMapper">

    <select id="selectSummaryListByAreaId"
            resultType="vip.appap.suxin.module.sales.service.bo.SalesPartnerStatisticsAreaStatisticsRespBO">
        SELECT area_id, COUNT(1) AS userCount
        FROM partner
        WHERE deleted = FALSE
        GROUP BY area_id
    </select>

    <select id="selectSummaryListBySex"
            resultType="vip.appap.suxin.module.sales.controller.admin.vo.SalesPartnerStatisticsSexStatisticsRespVO">
        SELECT sex, COUNT(1) AS userCount
        FROM partner
        WHERE deleted = FALSE
        GROUP BY sex
    </select>

    <select id="selectSummaryListByRegisterTerminal"
                         resultType="vip.appap.suxin.module.sales.controller.admin.vo.SalesPartnerStatisticsTerminalStatisticsRespVO">
        SELECT m.register_terminal as terminal, COUNT(1) AS userCount
        FROM partner p
        INNER JOIN member m ON p.id = m.id
        WHERE p.deleted = FALSE
        GROUP BY m.register_terminal
    </select>

    <select id="selectUserCount" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM partner
        WHERE deleted = FALSE
        <if test="beginTime != null">
            AND create_time >= #{beginTime}
        </if>
        <if test="endTime != null">
            AND create_time &lt;= #{endTime}
        </if>
    </select>

    <select id="selectListByCreateTimeBetween"
            resultType="vip.appap.suxin.module.sales.controller.admin.vo.SalesPartnerStatisticsRegisterCountRespVO">
        SELECT DATE_FORMAT(create_time, '%Y-%m-%d') AS date,
               count(1)                             AS count
        FROM partner
        WHERE create_time BETWEEN #{beginTime} AND #{endTime}
          AND deleted = FALSE
        GROUP BY date
    </select>

</mapper>
